# Server Configuration
PORT=8080

# gRPC Service Addresses
# Use host.docker.internal if the provider is running on your host machine (Mac/Windows)
# Use the service name if running in Docker Compose
# Use localhost only for running main.go directly on host
WEATHER_PROVIDER_ADDR=host.docker.internal:50051
