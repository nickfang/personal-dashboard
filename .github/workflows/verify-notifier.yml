name: Verify Notifier

on:
  pull_request:
    branches: [ main ]
    paths:
      - 'services/notifier/**'
      - 'services/shared/**'
      - '.github/workflows/verify-notifier.yml'

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Verify Dependencies
        working-directory: ./services/notifier
        run: go mod verify

      - name: Run Unit Tests
        working-directory: ./services/notifier
        run: go test -v ./...

      - name: Verify Build
        working-directory: ./services/notifier
        run: go build -v ./...
