name: Verify Forecast Collector

on:
  pull_request:
    branches: [ main ]
    paths:
      - 'services/forecast-collector/**'
      - 'services/shared/**'
      - '.github/workflows/verify-forecast-collector.yml'

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Verify Dependencies
        working-directory: ./services/forecast-collector
        run: go mod verify

      - name: Run Unit Tests
        working-directory: ./services/forecast-collector
        run: go test -v ./...

      - name: Verify Build
        working-directory: ./services/forecast-collector
        run: go build -v ./...
